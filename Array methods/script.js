const planets = ['Earth', 'Mars', 'Saturn', 'Mercury', 'Jupiter', 'Venus', 'Uranus', 'Neptune'];

const planetsString = planets.join(', ');

console.log(planetsString); // Outputs: 'Earth, Mars, Saturn, Mercury, Jupiter, Venus, Uranus, Neptune'

const saturnIndex = planets.indexOf('Saturn');

console.log(saturnIndex); // Outputs: 2